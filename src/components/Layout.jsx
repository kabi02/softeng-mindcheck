// Layout for the contents of each page
export default function Layout({children}) {
    return (
        <div className="w-full bg-[rgba(255,255,255,0.75)]  font-open-sans">
            {children}
        </div>
    )
}